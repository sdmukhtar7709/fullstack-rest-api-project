<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Quora Posts</title>
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
  <div class="container">
    <h1>Quora Posts</h1>
    <% for(post of posts){ %>
    <div class="post">
      <h3 class="user">@<%= post.username %></h3>
      <p class="content"><%= post.content %></p>
      <div class="actions">
        <a href="/posts/<%=post.id%>" class="btn btn-primary">See Details</a>
        <a href="/posts/<%=post.id%>/edit" class="btn btn-secondary">Edit</a>
        <form method="POST" action="/posts/<%= post.id %>?_method=DELETE" class="inline-form">
          <button type="submit" class="btn btn-danger">Delete</button>
        </form>
      </div>
    </div>
    <% } %>

    <a href="/posts/new" class="btn btn-create">+ Create New Post</a>
  </div>
</body>
</html>
